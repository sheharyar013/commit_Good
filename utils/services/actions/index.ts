import { getAreasOfInterest, getCoordinators } from "./campaigns";
import {
  loginUser,
  registerUser,
  getAllRegisteredEmails,
  getUserDetails,
  updateUserDetails,
  getNFTPrice,
} from "./auth";

import { getCampaigns } from "./campaigns";
import { getCountries } from "./county";
import { getWalletTokens } from "./wallet";

export {
  loginUser,
  registerUser,
  getCampaigns,
  getCountries,
  getWalletTokens,
  getAreasOfInterest,
  getCoordinators,
  getAllRegisteredEmails,
  getUserDetails,
  updateUserDetails,
  getNFTPrice,
};
